# Copy this file to `secrets.toml` and fill in your real keys.

# Weather skill — free key at: https://home.openweathermap.org/api_keys
OPENWEATHER_API_KEY = "your_openweather_api_key_here"

# Universal answers (LLM big brain) — free Gemini key at:
#   https://aistudio.google.com/apikey
GEMINI_API_KEY = "your_gemini_api_key_here"

# Optional fallback if Gemini fails or is rate-limited:
#   https://openrouter.ai/keys   (pick a free model; defaults to llama-3.3-70b)
OPENROUTER_API_KEY = ""

# Optional model overrides (advanced).
# GEMINI_MODEL = "gemini-2.5-flash"
# OPENROUTER_MODEL = "meta-llama/llama-3.3-70b-instruct:free"
